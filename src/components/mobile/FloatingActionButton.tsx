import React from "react";

/**
 * Simple Floating Action Button component
 * Disabled per user request - sync FAB not needed
 */
const FloatingActionButton: React.FC = () => {
  return null;
};

export default FloatingActionButton;
