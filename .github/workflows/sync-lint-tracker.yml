name: Sync Lint Tracker Workflow

on:
  push:
    branches: [develop]
    paths:
      - ".github/workflows/lint-warnings-tracker.yml"

  # Manual trigger option
  workflow_dispatch:
    inputs:
      force_sync:
        description: "Force sync even if no changes detected"
        required: false
        default: false
        type: boolean

# Prevent duplicate runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  issues: write

jobs:
  sync-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout develop branch
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Check for differences and sync
        run: |
          echo "🔍 Checking for differences between develop and main lint tracker workflows..."

          # Fetch latest main branch
          git fetch origin main

          # Compare lint tracker workflows
          DEVELOP_FILE=".github/workflows/lint-warnings-tracker.yml"

          if [ ! -f "$DEVELOP_FILE" ]; then
            echo "❌ Lint tracker workflow not found on develop branch"
            exit 1
          fi

          # Get main branch version of the file
          git show origin/main:$DEVELOP_FILE > main-lint-tracker.yml 2>/dev/null || echo "# File not found on main" > main-lint-tracker.yml

          # Compare files
          if ! diff -q "$DEVELOP_FILE" main-lint-tracker.yml > /dev/null; then
            echo "🔄 Differences detected! Syncing workflow from develop to main..."
            
            # Checkout main branch
            git checkout main
            git pull origin main
            
            # Copy the file from develop
            git checkout develop -- "$DEVELOP_FILE"
            
            # Check if there are actually changes to commit (check staged changes)
            if [ -n "$(git diff --cached --name-only)" ]; then
              echo "📝 Committing updated lint tracker workflow..."
              
              git add "$DEVELOP_FILE"
              git commit -m "sync: update lint-warnings-tracker workflow from develop [automated]"
              
              git push origin main
              echo "✅ Successfully synced lint tracker workflow to main branch!"
            else
              echo "ℹ️ No actual changes to commit after checkout"
            fi
          elif [ "${{ inputs.force_sync }}" == "true" ]; then
            echo "🔧 Force sync requested - updating main even without detected changes"
            git checkout main
            git pull origin main
            git checkout develop -- "$DEVELOP_FILE"
            
            if [ -n "$(git diff --cached --name-only)" ]; then
              git add "$DEVELOP_FILE"
              git commit -m "sync: force update lint-warnings-tracker workflow from develop [automated]"
              git push origin main
              echo "✅ Force sync completed!"
            else
              echo "ℹ️ Files are already identical - no sync needed"
            fi
          else
            echo "✅ Workflows are already in sync - no action needed"
          fi

      - name: Create or update issue comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            // Check if sync was needed/performed
            const wasPerformed = process.env.GITHUB_STEP_SUMMARY?.includes('Successfully synced') || false;

            const syncStatus = wasPerformed ? '✅ Sync Performed' : 'ℹ️ No Sync Needed';
            const trigger = context.eventName === 'workflow_dispatch' ? 'Manual' : 'Automatic';
            const comment = '🔄 **Lint Tracker Workflow Sync Status** - ' + new Date().toLocaleString('en-US', { timeZone: 'America/Chicago', timeZoneName: 'short' }) + '\n\n' + syncStatus + ': Lint tracker workflow sync completed\n\n**Monitored File**: `.github/workflows/lint-warnings-tracker.yml`\n**Trigger**: ' + trigger;

            // Post to issue #573 about the action improvements  
            try {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: 573,
                body: comment
              });
              console.log('✅ Posted sync status to issue #573');
            } catch (error) {
              console.log('⚠️ Could not post to issue #573:', error.message);
            }
