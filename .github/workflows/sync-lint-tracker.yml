name: Sync Lint Tracker Workflow

on:
  push:
    branches: [develop]
    paths:
      - '.github/workflows/lint-warnings-tracker.yml'
      - '.github/data/lint-warnings-comment.md'
      - 'scripts/parse-eslint-warnings.js'
  
  # Manual trigger option
  workflow_dispatch:
    inputs:
      force_sync:
        description: 'Force sync even if no changes detected'
        required: false
        default: 'false'
        type: boolean

jobs:
  sync-workflow:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout develop branch
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Check for differences and sync
        run: |
          echo "🔍 Checking for differences between develop and main lint tracker workflows..."
          
          # Fetch latest main branch
          git fetch origin main
          
          # Compare lint tracker files
          FILES_TO_SYNC=(
            ".github/workflows/lint-warnings-tracker.yml"
            ".github/data/lint-warnings-comment.md"
            "scripts/parse-eslint-warnings.js"
          )
          
          CHANGES_DETECTED=false
          
          for FILE in "${FILES_TO_SYNC[@]}"; do
            if [ ! -f "$FILE" ]; then
              echo "❌ File $FILE not found on develop branch"
              continue
            fi
            
            # Get main branch version of the file
            git show origin/main:$FILE > "main-$(basename $FILE)" 2>/dev/null || echo "# File not found on main" > "main-$(basename $FILE)"
            
            # Compare files
            if ! diff -q "$FILE" "main-$(basename $FILE)" > /dev/null; then
              echo "🔄 Changes detected in $FILE"
              CHANGES_DETECTED=true
            fi
          done
          
          if [ "$CHANGES_DETECTED" = true ]; then
            echo "🔄 Differences detected! Syncing workflow from develop to main..."
            
            # Checkout main branch
            git checkout main
            git pull origin main
            
            # Copy the files from develop
            for FILE in "${FILES_TO_SYNC[@]}"; do
              if [ -f "$FILE" ]; then
                git checkout develop -- "$FILE"
                echo "✅ Synced $FILE"
              fi
            done
            
            # Check if there are actually changes to commit
            if [ -n "$(git diff --name-only)" ]; then
              echo "📝 Committing updated lint tracker workflow..."
              
              git add "${FILES_TO_SYNC[@]}"
              git commit -m "sync: update lint tracker system from develop

This automated sync ensures parity between main and develop branches
for the lint tracking system.

Source: develop branch
Target: main branch
Files: workflow, template, parser script

🤖 Generated with automated sync action

Co-Authored-By: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
              
              git push origin main
              echo "✅ Successfully synced lint tracker workflow to main branch!"
            else
              echo "ℹ️ No actual changes to commit after checkout"
            fi
          elif [ "${{ inputs.force_sync }}" == "true" ]; then
            echo "🔧 Force sync requested - updating main even without detected changes"
            git checkout main
            git pull origin main
            
            for FILE in "${FILES_TO_SYNC[@]}"; do
              if [ -f "$FILE" ]; then
                git checkout develop -- "$FILE"
              fi
            done
            
            if [ -n "$(git diff --name-only)" ]; then
              git add "${FILES_TO_SYNC[@]}"
              git commit -m "sync: force update lint tracker system from develop"
              git push origin main
              echo "✅ Force sync completed!"
            else
              echo "ℹ️ Files are already identical - no sync needed"
            fi
          else
            echo "✅ Workflows are already in sync - no action needed"
          fi

      - name: Create or update issue comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            // Check if sync was needed/performed
            const wasPerformed = process.env.GITHUB_STEP_SUMMARY?.includes('Successfully synced') || false;
            
            const comment = `🔄 **Lint Tracker Workflow Sync Status** - ${new Date().toLocaleString('en-US', { 
              timeZone: 'America/Chicago',
              timeZoneName: 'short'
            })}

${wasPerformed ? '✅ **Sync Performed**: Lint tracker workflow updated on main branch from develop' : 'ℹ️ **No Sync Needed**: Workflows are already identical between branches'}

**Monitored Files**: 
- \`.github/workflows/lint-warnings-tracker.yml\`
- \`.github/data/lint-warnings-comment.md\`  
- \`scripts/parse-eslint-warnings.js\`
**Source Branch**: develop  
**Target Branch**: main
**Trigger**: ${context.eventName === 'workflow_dispatch' ? 'Manual' : 'Automatic (file change detected)'}

This ensures both branches have identical lint tracking capabilities with latest features and bug fixes.`;

            // Post to issue #573 about the action improvements  
            try {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: 573,
                body: comment
              });
              console.log('✅ Posted sync status to issue #573');
            } catch (error) {
              console.log('⚠️ Could not post to issue #573:', error.message);
            }